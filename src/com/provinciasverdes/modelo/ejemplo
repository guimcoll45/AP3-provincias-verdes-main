// Ejemplo validación de número
while (!scanner.hasNextDouble()) { 
    System.out.print("Ingrese un número válido: ");
    scanner.next();
}
double lat = scanner.nextDouble();
scanner.nextLine(); // Limpieza de buffer (corrección aplicada)
